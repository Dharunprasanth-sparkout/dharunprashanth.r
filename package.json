{
  "name": "dharunprashanth.r",
  "version": "1.0.0",
  "private": true,
  "description": "UI automation tests for DevMarket sign-in",
  "scripts": {
    "test:ui": "playwright test",
    "test:ui:headed": "playwright test --headed",
    "test:ui:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1"
  }
}
